{{> nav}}

<div class="listContainer">
{{#each productos}}
<div class="card">
  <h3>{{title}}</h3>
  <p>Price: {{price}}</p>
  <img src="{{thumbnail}}">
  <form action="/productos/eliminar/{{id}}" method="post">
    <input type="submit" value="Delete">
  </form>
</div>
{{/each}}
</div>